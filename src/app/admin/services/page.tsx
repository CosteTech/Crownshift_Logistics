import React from "react";

export const metadata = {
  title: "Admin — Services",
};

export default async function ServicesAdminPage() {
  return (
    <div>
      <h1>Services</h1>
      <p>Manage services (create, edit, delete) from this interface.</p>
    </div>
  );
}
